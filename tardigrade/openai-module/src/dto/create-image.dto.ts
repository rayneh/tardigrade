// src/dtos/create-image.dto.ts

export class CreateImageDto {
  image1: string; // Base64 encoded image
  image2: string; // Base64 encoded image
  featureIndex: number; // Index for selecting additional feature
}
